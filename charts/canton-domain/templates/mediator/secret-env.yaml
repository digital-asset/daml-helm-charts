{{- if or .Values.common.environmentSecrets .Values.mediator.environmentSecrets }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "canton-node.fullname" . }}-mediator-secretmap
  labels: {{- include "canton-node.labels" (list . "mediator") | nindent 4 }}
    {{- with .Values.commonLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
type: Opaque
data:
{{- range $key,$value := mustMergeOverwrite .Values.common.environmentSecrets .Values.mediator.environmentSecrets }}
  {{ $key }}: {{ $value | b64enc | quote }}
{{- end }}
{{- end }}
